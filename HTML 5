<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plantify Log In</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #E6F4F1;
        }

        .container {
            width: 320px;
            padding: 20px;
            border-radius: 15px;
            background-color: #FFFFFF;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        .header {
            margin-bottom: 20px;
        }

        .header h1 {
            font-size: 24px;
            margin: 10px 0;
            color: #5A5A5A;
        }

        .header h2 {
            font-size: 18px;
            margin: 5px 0;
            color: #2D6A4F;
        }

        .plant-icon {
            margin-bottom: 10px;
        }

        .form-group {
            margin: 10px 0;
            text-align: left;
        }

        .form-group label {
            display: block;
            font-size: 14px;
            color: #5A5A5A;
            margin-bottom: 5px;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #CCC;
            border-radius: 25px;
            background-color: #F7F9F9;
            text-align: center;
        }

        .form-group input[type="email"],
        .form-group input[type="password"] {
            background-color: #FFDDD2;
            color: #5A5A5A;
        }

        .buttons {
            margin: 20px 0;
        }

        .buttons button {
            width: 100%;
            padding: 10px;
            background-color: #FFAB91;
            border: none;
            border-radius: 25px;
            color: white;
            font-weight: bold;
            cursor: pointer;
        }

        .buttons button:hover {
            background-color: #FF9170;
        }

        .footer {
            font-size: 12px;
            color: #5A5A5A;
        }

        .footer a {
            color: #FF6F61;
            text-decoration: none;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header Section -->
        <div class="header">
            <div class="plant-icon">
                <img src="plant-icon.png" alt="Plantify" width="60">
            </div>
            <h1>Sign in to continue</h1>
            <h2>Plantify</h2>
        </div>

        <!-- Form Section -->
        <form>
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" value="hello@reallygreatsite.com" readonly>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" placeholder="Enter your password">
            </div>
            <div class="buttons">
                <button type="submit">Login</button>
            </div>
        </form>

        <!-- Footer Section -->
        <div class="footer">
            <p><a href="#">Forgot Password?</a></p>
        </div>
    </div>
</body>
</html>
